# Orquestração
dagster==1.8.*
dagster-webserver==1.8.*
# dagster-gcp e dagster-dbt NÃO são usadas -- ver resources.py e a
# docstring de assets.py::dbt_build pro motivo (dbt chamado via
# subprocess direto, clientes GCP via ingestion/loaders.py já existente).

# Transformação
dbt-core==1.8.*
dbt-bigquery==1.8.*

# Ingestão / HTTP / scraping
requests==2.32.*
tenacity==9.0.*
beautifulsoup4==4.12.*
lxml==5.3.*

# Dados
pandas==2.2.*
pyarrow==17.0.*

# GCP
google-cloud-bigquery==3.25.*
google-cloud-storage==2.18.*

# Config / utilidades
python-dotenv==1.0.*
pydantic==2.9.*
structlog==24.4.*

# Testes
pytest==8.3.*
pytest-mock==3.14.*
